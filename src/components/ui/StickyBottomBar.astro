---
import { practice } from '../../data/site';
---

<div class="fixed inset-x-0 bottom-0 z-40 bg-white/95 backdrop-blur border-t border-gray-200 shadow-card md:hidden">
  <div class="mx-auto flex max-w-5xl items-center gap-3 px-4 py-3">
    <a href={practice.phoneHref} class="flex-1 text-center rounded-full bg-primary text-white px-4 py-2 font-semibold">Call</a>
    <a href={practice.primaryCTA.href} class="flex-[1.5] text-center rounded-full bg-secondary text-white px-4 py-2 font-semibold">Request Appointment</a>
  </div>
</div>
